/**
 * NLF Bookmarks Widget
 * No API required - renders bookmark icons from config
 */

const widget = {
  // No API calls needed - this is a static/config-based widget
  api: null,
  proxyHandler: null,
};

export default widget;
